
// This file was part of a React setup and is no longer used for the vanilla JavaScript version of "Le Poisson Steve".
// The game logic is now handled by script.js.
// Keeping this file with commented content to prevent React initialization errors if it's still processed.

/*
import React from 'react';
import ReactDOM from 'react-dom/client';
import App from './App';

const rootElement = document.getElementById('root');
if (!rootElement) {
  throw new Error("Could not find root element to mount to");
}

const root = ReactDOM.createRoot(rootElement);
root.render(
  <React.StrictMode>
    <App />
  </React.StrictMode>
);
*/
